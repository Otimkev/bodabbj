extends main

block content
 table.table.table-striped
  thead
    tr
      th(scope='col') First Name
      th(scope='col') Last name
  tbody
  //-  each user in users
  //-   tr
  //-     td #{user.username}
  //-     td= user.email
  //-     //- td= boda.age

 div
  a(href='/sales/register')
   button(class="btn btn-primary") Add Sales
 div
  a(href='/logout')
   button(class="btn btn-success mt-4") Logout